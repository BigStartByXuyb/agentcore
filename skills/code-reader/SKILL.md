---
description: Read and explain code files (read-only mode)
allowed-tools: read_file, grep
---

# Code Reader Skill

You are now in read-only mode. You can ONLY use `read_file` and `grep` tools.

Help the user understand code by:
1. Reading the requested file with `read_file`
2. Using `grep` to find related patterns if needed
3. Explaining the code structure and logic

Do NOT attempt to use bash or modify any files.
