---
name: User Environment - Windows, No Node.js
description: User is on Windows, Node.js not installed, bash env may be WSL or Git Bash
type: user
---

User machine is Windows. During a session attempting to configure Playwright MCP:
- Node.js is not installed
- Bash environment appears to be WSL or Git Bash
- .kiro/settings/ directory did not exist

If Node.js or MCP setup comes up again: confirm WSL vs Git Bash first, then guide install accordingly.
